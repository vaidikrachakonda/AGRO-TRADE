import { ArrowRight, Check, KeyRound, Sprout, UserRound } from 'lucide-react';
import { useState } from 'react';
import { Link, useLocation } from 'wouter';

export default function Login() {
  const [, setLocation] = useLocation();
  const [role, setRole] = useState<'buyer' | 'farmer'>('buyer');
  const [mode, setMode] = useState<'login' | 'register'>('login');
  const [phone, setPhone] = useState('');
  const [name, setName] = useState('');
  return <div className="min-h-[100dvh] bg-[#dfe8d5]"><div className="mx-auto grid min-h-[100dvh] max-w-7xl lg:grid-cols-[.85fr_1.15fr]"><section className="relative hidden overflow-hidden p-10 lg:flex lg:flex-col lg:justify-between"><div className="leaf-grid absolute inset-0 opacity-40" /><div className="relative"><Link href="/" data-testid="link-login-brand" className="inline-flex items-center gap-3 text-primary"><span className="grid size-10 place-items-center rounded-xl bg-primary text-primary-foreground"><Sprout size={20} /></span><span><span className="block font-serif text-xl font-semibold">AGRO TRADE</span><span className="font-mono text-[9px] uppercase tracking-[.2em]">grown close · shared fairly</span></span></Link></div><div className="relative max-w-md"><p className="font-mono text-[10px] uppercase tracking-[.22em] text-accent">a better exchange</p><h1 className="mt-4 font-serif text-6xl font-semibold leading-[.95] tracking-tight text-primary">The best food<br /><em className="font-normal text-accent">has a place</em><br />you can point to.</h1><p className="mt-7 max-w-sm text-sm leading-7 text-primary/70">Your local produce exchange, built around the people who grow and cook it.</p></div><div className="relative flex items-center gap-3 text-xs font-semibold text-primary"><span className="grid size-8 place-items-center rounded-full bg-primary text-primary-foreground"><Check size={15} /></span> Transparent by design · trusted by neighbours</div></section><section className="flex items-center justify-center bg-card px-4 py-10 sm:px-8"><div className="w-full max-w-md"><Link href="/" data-testid="link-mobile-login-brand" className="mb-10 flex items-center gap-2 text-primary lg:hidden"><Sprout size={20} /><span className="font-serif text-xl font-semibold">AGRO TRADE</span></Link><p className="font-mono text-[10px] uppercase tracking-[.22em] text-accent">your market, your way</p><h2 className="mt-3 font-serif text-4xl font-semibold tracking-tight">{mode === 'login' ? 'Welcome back.' : 'Join the exchange.'}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Sign in as a {role === 'buyer' ? 'neighbour' : 'farmer'} to pick up where you left off.</p><div className="mt-7 grid grid-cols-2 gap-2 rounded-xl bg-muted p-1"><button type="button" data-testid="button-role-buyer" onClick={() => setRole('buyer')} className={`flex items-center justify-center gap-2 rounded-lg py-2.5 text-sm font-semibold ${role === 'buyer' ? 'bg-card text-primary shadow-sm' : 'text-muted-foreground'}`}><UserRound size={15} /> Buyer</button><button type="button" data-testid="button-role-farmer" onClick={() => setRole('farmer')} className={`flex items-center justify-center gap-2 rounded-lg py-2.5 text-sm font-semibold ${role === 'farmer' ? 'bg-card text-primary shadow-sm' : 'text-muted-foreground'}`}><Sprout size={15} /> Farmer</button></div><form onSubmit={(event) => { event.preventDefault(); setLocation(role === 'farmer' ? '/farmer' : '/'); }} className="mt-7"><label className="block text-xs font-semibold uppercase tracking-wide text-muted-foreground">{mode === 'register' ? 'Your name' : 'Name or phone'}<input required data-testid="input-login-name" value={mode === 'register' ? name : phone} onChange={(event) => mode === 'register' ? setName(event.target.value) : setPhone(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm normal-case tracking-normal outline-none focus:ring-2 focus:ring-ring" placeholder={mode === 'register' ? 'e.g. Asha Mehta' : 'e.g. +91 98765 43210'} /></label>{mode === 'register' && <label className="mt-4 block text-xs font-semibold uppercase tracking-wide text-muted-foreground">Mobile number<input required data-testid="input-register-phone" value={phone} onChange={(event) => setPhone(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm normal-case tracking-normal outline-none focus:ring-2 focus:ring-ring" placeholder="+91 98765 43210" /></label>}<button type="submit" data-testid="button-login-submit" className="mt-5 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-primary font-semibold text-primary-foreground transition hover:-translate-y-0.5">{mode === 'login' ? 'Continue securely' : 'Create my account'} <ArrowRight size={16} /></button></form><button type="button" data-testid="button-toggle-auth-mode" onClick={() => setMode(mode === 'login' ? 'register' : 'login')} className="mt-6 flex w-full items-center justify-center gap-2 text-sm font-semibold text-primary">{mode === 'login' ? 'New to the exchange? Create an account' : 'Already have an account? Sign in'}<KeyRound size={14} /></button><p className="mt-9 text-center text-[11px] leading-5 text-muted-foreground">By continuing, you agree to keep this exchange respectful and fair for every farm and household.</p></div></section></div></div>;
}
